# ─── InvoiceShield Contracts (Foundry) ─────────────────────────────────
# Copia este archivo a .env y completa los valores. NUNCA subas .env al repo.

# Private key del deployer (misma que ARBITRUM_PRIVATE_KEY del backend).
# Debe estar financiada con ETH de Arbitrum Sepolia.
PRIVATE_KEY=""

# RPC de Arbitrum Sepolia (Alchemy).
RPC_URL=""

# Opcional: clave API de Arbiscan para verificar el contrato (forge verify).
ETHERSCAN_API_KEY=""
